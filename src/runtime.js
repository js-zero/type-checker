
$assume `Object : {
  let: ({ t }, { s }) => { ...t, ...s }
}`
// LAST TIME: Update grammar to allow for Object.let : { ... }
// LAST TIME: Update grammar to allow for Object.let : { ... }
// LAST TIME: Update grammar to allow for Object.let : { ... }
// LAST TIME: Update grammar to allow for Object.let : { ... }
// LAST TIME: Update grammar to allow for Object.let : { ... }
// LAST TIME: Update grammar to allow for Object.let : { ... }
// LAST TIME: Update grammar to allow for Object.let : { ... }
// LAST TIME: Update grammar to allow for Object.let : { ... }
// LAST TIME: Update grammar to allow for Object.let : { ... }
// LAST TIME: Update grammar to allow for Object.let : { ... }
// LAST TIME: Update grammar to allow for Object.let : { ... }
